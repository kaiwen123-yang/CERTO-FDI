你现在是 CERTO-FDI Paper Reset V2-R 的执行负责人。

本轮不是继续设计新网络，而是完成：

1. ME-AD 真实渐进故障闭环；
2. AURSAD 原生协议 vs 泄漏安全协议；
3. 四数据集真实统一基线矩阵；
4. 自包含全文证据；
5. 算法、benchmark artifact、投稿成熟度三轴判定。

请在以下有限位置寻找本执行包：

```text
当前目录
~/Downloads
~/Desktop
/mnt/c/Users/*/Downloads
/mnt/c/Users/*/Desktop
/mnt/g/CERTO-FDI/00_inbox
```

寻找：

```text
CERTO_FDI_V2R_MEAD_BENCHMARK_SUBMISSION_CLOSURE_20260824.zip
01_MASTER_PROMPT.md
```

找到 ZIP 后：

1. 计算 SHA256；
2. `unzip -t`；
3. 解压到 `/tmp/certo_v2r_<UTC>`；
4. 完整读取 `01_MASTER_PROMPT.md`；
5. 将其作为唯一执行合同；
6. 立即执行，不只列计划。

Git：

```text
remote:
git@github.com:kaiwen123-yang/CERTO-FDI.git

base:
stage/paper-reset-v2-500x100-public-story-convergence

head:
stage/paper-reset-v2r-mead-submission-closure

worktree:
~/research/CERTO-FDI-WORKTREES/paper-reset-v2r-mead-submission-closure
```

PR #1–#9 保持 Draft、未合并、head 不动。

已知 PR #9 head（开始时重新核验）：

```text
e8ca63983d0738e7540d82200e551d20a8fca681
```

寻找并验证：

```text
CERTO_FDI_V2_FULL_REVIEW_20260824T065812Z.zip
expected SHA256:
7cb14c7888e7768f6fd84120c1c2eac8cab4ce22ee2ce0af65b2a32338e5b73f
```

所有大文件写入：

```text
/mnt/g/CERTO-FDI
```

绝对禁止：

```text
第三种 LiGRA
恢复 gauge-breaking
旧 MuJoCo 调参
final test 调参
补造公共数据物理量
自动 merge
force-push
```

本轮最终必须给出：

```text
algorithm_state
benchmark_artifact_state
submission_readiness
combined_state
```

开始时先报告环境、G 盘、Git、PR #9、V2 包、数据集、ME-AD、文献卡、RUN_ID；然后立即冻结协议并持续执行至终态。
