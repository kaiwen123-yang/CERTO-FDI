"""CERTO-FDI Paper Reset V2: 500-screen, 100-fulltext, public-benchmark and
paper-story convergence round. Audit/convergence code only; never imports
``certo_fdi`` (the internal simulation stack) and only builds on
``certo_fdi_reset`` where provenance is explicit."""

__version__ = "2.0.0"
